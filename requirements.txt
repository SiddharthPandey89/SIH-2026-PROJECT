# Sovereign AI Workbench
# Python dependencies for the currently implemented backend,
# knowledge-base ingestion, and local vector store.

# ---------------------------------------------------------
# Web framework / ASGI server
# ---------------------------------------------------------
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# ---------------------------------------------------------
# Validation / schemas
# ---------------------------------------------------------
pydantic>=2.7,<3.0

# ---------------------------------------------------------
# Local model-server communication
# ---------------------------------------------------------
httpx>=0.27,<1.0

# ---------------------------------------------------------
# Database
# ---------------------------------------------------------
sqlalchemy>=2.0,<3.0
aiosqlite>=0.20,<1.0

# ---------------------------------------------------------
# File upload support
# ---------------------------------------------------------
python-multipart>=0.0.9,<1.0

# ---------------------------------------------------------
# Document ingestion
# ---------------------------------------------------------
pypdf>=4.0,<6.0
python-docx>=1.0,<2.0

# ---------------------------------------------------------
# Local vector database
# ---------------------------------------------------------
chromadb>=1.0,<2.0

# ---------------------------------------------------------
# Local embedding / semantic search
# ---------------------------------------------------------
sentence-transformers>=3.0,<6.0

# ---------------------------------------------------------
# Local in-process Mistral 7B (transformers path)
# Install a CUDA wheel of torch when a GPU is available.
# ---------------------------------------------------------
torch>=2.2,<3.0
transformers>=4.40,<5.0
accelerate>=0.30,<2.0
safetensors>=0.4,<1.0

# Optional: GGUF / llama.cpp backend for CPU-friendly Mistral weights
# llama-cpp-python>=0.2.90,<0.4